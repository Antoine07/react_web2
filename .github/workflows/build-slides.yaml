# name: Build Marp Slides
#
# Ce fichier est conservé à titre de référence,
# mais toutes les actions GitHub sont désactivées.
# Aucune exécution automatique ni déploiement.
#
# Pour regénérer les slides :
#   npm run slides:build
# Pour prévisualiser :
#   npm run slides:watch
#
# Si besoin de réactiver la CI/CD GitHub :
#   décommente les sections "on:" et "jobs:"
#
# --- AUCUNE ACTION GITHUB ACTIVE ---
#
# on:
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#
#       - name: Install Marp CLI
#         run: npm install -g @marp-team/marp-cli
#
#       - name: Build slides to docs/
#         run: |
#           mkdir -p docs
#           for file in Slides/*.md; do
#             marp "$file" --html --allow-local-files -o "docs/$(basename "${file%.md}.html")"
#           done
#
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#
#       - name: Setup Pages
#         uses: actions/configure-pages@v4
#
#       - name: Upload docs directory
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: docs
#
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
#
#       - name: Display deployed URL
#         run: echo "✅ Site déployé sur ${{ steps.deployment.outputs.page_url }}"
